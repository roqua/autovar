machine:
  environment:
    R_LIBS_USER: ~/lib/R/library
    _R_CHECK_FORCE_SUGGESTS_: FALSE
    _R_CHECK_ASCII_DATA_: FALSE
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - if [[ -e ~/docker/image.tar ]]; then docker load -i ~/docker/image.tar; fi
    - docker build -t roqua/autovar-test -f test.Dockerfile .
    - mkdir -p ~/docker; docker save roqua/autovar-test > ~/docker/image.tar

test:
  override:
    - docker run roqua/autovar-test R CMD check /autovar/autovar_0.2-0.tar.gz --no-manual --no-build-vignettes
